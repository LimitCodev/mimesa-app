{
  "name": "mimesa",
  "version": "1.0.0",
  "description": "MiMesa - mesa de trabajo web 2",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT",
    "api": "json-server --watch db.json --port 3000",
    "web": "npx live-server --port=5500"
  },
  "dependencies": {
    "json-server": "0.17.4"
  },
}
